<div>
  <h3><%= @user.name %>MyPage</h3>
  <%= attachment_image_tag @user, :pf_image, :fill, 100, 100, format: 'jpeg', fallback: "user.jpg" %>
  <% if @user.id == current_user.id %>
    <p><%= link_to "プロフィール編集", edit_user_path(@user) %></p>
  <% end %>
</div>
